//logo, contact info, nav bar
import React from 'react';
import NavBar from '../components/NavBar.jsx';

const HeaderContainer = () => {
    return(
            <div>
                <h2>just imagine there's a header here</h2>
                <NavBar />
            </div>
    );
};

export default HeaderContainer;